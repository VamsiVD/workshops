$kind: http-request
url: '{{base_url}}/notices/{{notice_id}}'
method: DELETE
scripts:
  - type: afterResponse
    code: |-
      pm.test('status is 200', () => pm.response.to.have.status(200));
      pm.test('delete message', () => pm.expect(pm.response.json().message).to.eql('Notice deleted successfully'));
    language: text/javascript
order: 6000
